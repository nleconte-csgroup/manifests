# Gateway API configuration with rule name
gatewayApi:
  enabled: true
  parentRefs:
  - kind: Gateway
    name: test-gateway
  rules:
    - name: service
      matches:
        - path:
            type: PathPrefix
            value: /
    - name: metrics
      matches:
        - path:
            type: PathPrefix
            value: /metrics
